// forEach Iterator using lamda expression, toString() with JSON Object
		ArrayList<Employee> aLEmployee = new ArrayList<Employee>();
        aLEmployee.forEach(emp -> System.out.println(emp.toString(true)));